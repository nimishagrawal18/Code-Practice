package Leet;

public class ZigZag {
    public String convert(String s, int numRows) {
        if(numRows==1) return s;
        // StringBuffer out=new StringBuffer("");
        int len=s.length();
        int factor=numRows+numRows-2;
        int mul = 0;
        do {

        }while (mul*factor<len);
        return "hi";
    }
}
